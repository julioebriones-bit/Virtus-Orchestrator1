{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "crons": [
    {
      "path": "/api/daily-automation",   // ← Cambia si tu handler está en otro path (ej. /api/cron/daily)
      "schedule": "5 6 * * *"            // 6:05 UTC = 12:05 AM CST (México)
    }
  ]
}
